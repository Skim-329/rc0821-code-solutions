/* exported last */
/* Pseudo Code
Define a function named last with a single parameter array.
Look at the last element of the array.
Return the last element of array.
*/
function last(array) {
  return array[array.length - 1];
}
