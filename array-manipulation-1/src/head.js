/* exported head */
/* Pseudo Code
Define a function named head with a parameter of array.
Look at the first element of the array.
Return the first element of array.
*/

function head(array) {
  return array[0];
}
