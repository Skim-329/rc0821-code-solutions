/* exported chunk */

/*
function chunk(array, size) {
  var newArray = [];
  var sizeArray = [];
  var subArray = [];

  for (var i = 0; i < size; i++) {
    var x = array[i];
    var y =
    if (array.length > 0) {
      sizeArray.push(x);
    } else {
      sizeArray.push();
    }
  }
  return newArray;
}
*/
